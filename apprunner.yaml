version: 1.0
runtime: python311
build:
  commands:
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
run:
  command: streamlit run meeting_agent.py --server.port 8501 --server.address 0.0.0.0
  network:
    port: 8501
